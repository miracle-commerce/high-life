{% comment %}
    Renders a radio input.
    Accepts:
    - container_class: {String} apply any custom classes to the input/label container (optional)
    - id: {String} ID to be applied to the input
    - classes: {String} custom classes to be applied to the input (optional)
    - value: {String} the input value (optional)
    - name: {String} the name attribute of the input
    - attributes: {String} add custom element attributes (data-* or aria)
    - label_text: {String} the text to display as the label
    - label_class: {String} any custom classes for the label element
    - data_aid: {String} custom data aid

    Usage:
      {% render 'armament-radio-item' with id: 'option1', name: 'options', value: 'option1', attributes: 'checked', label_text: 'Option 1' %}
{% endcomment %}

<div class="flex items-center pb-s last:pb-0{% if container_class %} {{ container_class }}{% endif %}">
  <input
    type="radio"
    {% if name %}name="{{ name }}"{% endif %}
    {% if id %}id="{{ id }}"{% endif %}
    {% if classes %}class="{{ classes }}"{% endif %}
    {% if value %}value="{{ value }}"{% endif %}
    {% if attributes %}{{ attributes }}{% endif %}
    data-aid="{{ data_aid }}"
  >
  <label class="pl-l inline-block w-full {{ label_class }}" for="{{ id }}"{% if label_attributes %} {{ label_attributes }}{% endif %}>{{ label_text }}</label>
</div>